{
  "name": "@gradtracker/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Express API, domain pipeline, and the Gmail/classifier ports.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "db:generate:pg": "drizzle-kit generate --config=drizzle.config.pg.ts",
    "db:generate:sqlite": "drizzle-kit generate --config=drizzle.config.sqlite.ts"
  },
  "dependencies": {
    "@gradtracker/shared": "*",
    "@libsql/client": "^0.17.4",
    "drizzle-orm": "^0.45.2",
    "zod": "^3.23.8"
  }
}
