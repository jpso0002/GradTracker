4px track showing how sure the model is about an extracted field — always paired with the value it qualifies, never shown alone.

```jsx
<ConfidenceMeter value={0.92} />
```

>=80% indigo · 55–79% amber · below 55% grey (and the field should be marked "Needs review").
