Date / countdown chip in tabular figures — urgency comes from `daysLeft`, not from the caller picking a colour.

```jsx
<DeadlinePill daysLeft={2}>Due in 2 days</DeadlinePill>
<DeadlinePill daysLeft={21}>14 Sep</DeadlinePill>
```
