Centred zero state: icon disc, one line of what's missing, one action.

```jsx
<EmptyState icon="mail-search" title="No applications yet"
  description="Connect Gmail and GradTracker builds your pipeline in about a minute."
  action={<Button iconLeft="mail">Connect Gmail</Button>} />
```
