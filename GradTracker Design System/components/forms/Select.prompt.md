Native select in GradTracker chrome — same geometry as `Input`, trailing chevron.

```jsx
<Select label="Stage" value={stage} onChange={setStage}
  options={["Applied","Assessment pending","Interview scheduled"]} />
```
