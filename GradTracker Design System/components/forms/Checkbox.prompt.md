18px indigo checkbox, 4px radius. Use for multi-select lists and consent rows.

```jsx
<Checkbox checked={on} onChange={setOn} label="Email me deadline reminders"
  description="One digest each morning, never more." />
```
