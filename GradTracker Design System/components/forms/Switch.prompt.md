Settings toggle — label left, 36×20 track right. Takes effect immediately (no Save button pattern).

```jsx
<Switch checked={on} onChange={setOn} label="Auto-detect applications"
  description="GradTracker reads new mail every 15 minutes." />
```
