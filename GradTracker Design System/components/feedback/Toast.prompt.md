Bottom-left confirmation on a white card — the tone shows only in the icon colour, never as a coloured fill.

```jsx
<Toast tone="success" action={<Button variant="ghost" size="sm">Undo</Button>}>Moved to Interview scheduled</Toast>
```
