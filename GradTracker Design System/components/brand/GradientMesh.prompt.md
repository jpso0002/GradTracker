Wraps a marketing hero and paints the mesh behind it. Bare-canvas heroes are off-brand.

```jsx
<GradientMesh height={440}>
  <SiteHeader /> <Hero />
</GradientMesh>
```

Never put the mesh behind product UI (app surfaces stay flat white / navy), and never place body copy on the mesh's saturated right end.
