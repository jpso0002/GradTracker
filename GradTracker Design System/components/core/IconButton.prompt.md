Square-ish icon-only control for row affordances, toolbars, and dismissals.

```jsx
<IconButton icon="archive" label="Archive application" />
<IconButton icon="star" label="Pin" active />
```

Ghost by default (chrome appears on hover only). `variant="outlined"` when it sits alone on a card with no other affordance nearby.
